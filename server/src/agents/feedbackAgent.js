export function buildAnalytics({ applications, matches, internships, profile }) {
  // TODO: Compute totalApplications, interviewRate, offerRate, the average match score of
  // TODO: applied roles, the top performing skills, and the recommendation note.
  return {
    totalApplications: 0,
    interviewRate: 0,
    offerRate: 0,
    matchScoreEffectiveness: 0,
    topPerformingSkills: [],
    recommendationNote: ""
  };
}
