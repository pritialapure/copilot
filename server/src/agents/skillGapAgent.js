export function buildSkillGapReport(match) {
  // TODO: Turn the match's missing skills into prioritized actions (first two High, rest
  // TODO: Medium) with a suggested action and a mini project for each.
  return { internshipId: match?.internshipId, missingSkills: [], priorities: [] };
}
